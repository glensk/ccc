Show how this Claude Code session's AIM (its "done-condition") evolved — every (re)definition, from the first one to the current, with each revision's specificity score.

Usage: /aim-history

Run this exact command (`ccc` resolves the current session from the working directory):

```
ccc aim-history
```

Then, in one short line, summarize the progression — how the AIM got sharper over time (what became concrete, e.g. a named test / command / artifact replacing a vague verb). Do not change the AIM; this is read-only.
